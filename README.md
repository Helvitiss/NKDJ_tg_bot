# DailyPulse Bot

> Production-ready Telegram-бот для ежедневных опросов команды с автоматическим скорингом, отчётами админу и отправкой в рабочий чат.

## Как назвать репозиторий

Рекомендую использовать имя:

**`dailypulse-bot`**

Почему это хорошее имя:
- короткое и легко читается;
- отражает суть (daily + pulse/check-in);
- нейтрально для дальнейшего развития проекта;
- хорошо выглядит в URL и Docker/CI окружении.

---

## Что умеет бот

- Ежедневно планирует отложенную отправку опроса на **20:00 по локальному часовому поясу пользователя**.
- Если пользователь уже прошёл опрос до этого времени, отложенное сообщение автоматически пропускается.
- Поддерживает **досрочный запуск** опроса командой `/result`.
- Поддерживает **изолированный тестовый сценарий** `/test` (не влияет на боевые данные).
- Перед отправкой анкеты показывает **экран подтверждения**:
  - ✅ Подтвердить
  - ✏️ Заполнить заново
- Считает итоговую эффективность через отдельный `ScoringEngine`.
- Отправляет отчёты:
  - администратору;
  - в дополнительный чат/группу (`REPORT_CHAT_ID`, опционально).
- Если пользователь не ответил за 12 часов — отправляет уведомление.
- Админ может удалить пользователя из рассылки командой `/remove_user <telegram_user_id>`.

---

## Технологии

- Python 3.12
- aiogram v3
- SQLAlchemy 2.0 (async)
- PostgreSQL
- APScheduler (async)
- FSM (aiogram)
- Docker + docker-compose

---

## Быстрый старт (из коробки)

### 1) Клонировать репозиторий

```bash
git clone <your_repo_url>
cd dailypulse-bot
```

### 2) Создать `.env`

```bash
cp .env.example .env
```

### 3) Заполнить переменные окружения

Обязательные:
- `BOT_TOKEN`
- `ADMIN_ID`

Опциональные:
- `REPORT_CHAT_ID` — ID группы/чата для дублирования отчётов
- `DATABASE_URL` — если не указать, используется дефолтная Postgres из `docker-compose`

Пример:

```env
BOT_TOKEN=your_bot_token
ADMIN_ID=123456789
REPORT_CHAT_ID=-1001234567890
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/postgres
```

### 4) Запустить проект

```bash
docker compose up --build
```

После запуска:
- поднимется PostgreSQL;
- дождётся healthcheck базы;
- стартует бот;
- таблицы создадутся автоматически на startup.

---

## Архитектура проекта

Проект построен по layered architecture:

- `bot/handlers` — только Telegram-взаимодействие
- `bot/services` — бизнес-логика
- `bot/repositories` — работа с БД
- `bot/domain` — доменный скоринг
- `bot/scheduler` — фоновые задачи
- `bot/keyboards` — Telegram UI
- `bot/config` — настройки через pydantic settings

---

## Сценарий опроса

Каждый день в 20:00 по локальному времени пользователя:

1. Настроение (🟢 / 🟡 / 🔴)
2. Твой режим: масштабирование или тест
3. Сколько компаний запустил
4. Сколько гео запустил
5. Подходы по крео
6. Сколько кабинетов
7. Подтверждение перед отправкой

После завершения:
- пользователь получает результат;
- админ/чат получает структурированный отчёт.

---

## Команды бота

- `/start` — регистрация пользователя и приветствие
- `/timezone Europe/Warsaw` — установить таймзону через IANA
- `/timezone +1` — установить смещение от UTC
- `/result` — запустить сегодняшний опрос сразу
- `/test` — тестовый опрос (не сохраняется в боевую статистику)
- `/stats [day|week|month]` — статистика по пользователям и общая за период (только для админа)
- `/remove_user <telegram_user_id>` — удалить пользователя (только для админа)

---

## Docker Compose

В `docker-compose.yml` реализовано:
- `postgres` + `bot` сервисы;
- healthcheck для Postgres;
- запуск бота после готовности БД;
- `restart: unless-stopped`;
- volume для хранения данных БД.

---

## Планы по развитию

- миграции Alembic;
- unit/integration tests;
- экспорт отчётов в CSV/Google Sheets;
- веб-админка для управления пользователями и статистикой.
